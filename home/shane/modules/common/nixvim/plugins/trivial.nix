_:
{
  # Plugins that only need `enable = true` — collapsed here to avoid
  # 8-line-per-file proliferation. Move out into a dedicated file the
  # moment one of these needs real configuration.
  plugins = {
    lazydev.enable = true;
    lazygit.enable = true;
    lualine.enable = true;
    luasnip.enable = true;
    notify.enable = true;
    nui.enable = true;
    web-devicons.enable = true;
  };
}
